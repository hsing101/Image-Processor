package ca.ubc.ece.cpen221.ip.mp;

import ca.ubc.ece.cpen221.ip.core.Image;

import org.junit.jupiter.api.Test;

import java.awt.*;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class Task4Tests {


    /**public void test_GreenScreen() {
        // Load the original image and the expected image
        Image originalImg = new Image("resources/melania-green-screen.jpg");
        Image backgroundImg = new Image("resources/25098.jpg");
        // Create an instance of ImageTransformer
        ImageTransformer transformer = new ImageTransformer(originalImg);

        // Apply the block paint with a specified block size
        Image outputImage = transformer.greenScreen(Color.GREEN, backgroundImg);  // Use the block size as required

        // Save the output image using the save method from the Image class
        outputImage.save("resources/tests/output_green_Screened_Image.png"); // Path to save the output image

    }*/
}